$schema: https://json-schema.org/draft/2020-12/schema
title: receiver_creator receiver configuration
type: object
properties:
    discovery:
        type: object
        properties:
            default_annotations:
                type: object
                additionalProperties:
                    type: string
            enabled:
                type: boolean
            ignore_receivers:
                type: array
                items:
                    type: string
        additionalProperties: false
    resource_attributes:
        description: |-
            ResourceAttributes is a map of resource attributes to add to just this receiver's resource metrics.
            It can contain expr expressions for endpoint env value expansion
        type: object
    watch_observers:
        description: WatchObservers are the extensions to listen to endpoints from.
        type: array
        items:
            type: string
additionalProperties: false
